import React from 'react';
import styles from './game_controller.module.css';
/**
 * TODO
 * enable to change the game, move to the left side that is the separated display
 */
const GameController = (props) => <h1>game controller</h1>;

export default GameController;
